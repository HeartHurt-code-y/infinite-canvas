# AI 图片与视频生成画布

一个以 AI 图片和视频生成为核心的节点式空间创作环境。用户在生成节点内编写提示词、连接参考素材来组装生成请求，并以生成结果继续创作。

## Language

**画布**：
承载一次视觉创作过程的空间工作区。新建画布初始为空，用户从素材库拖入素材、从节点仓库拖入生成节点，并自行建立创作关系。
_Avoid_: 白板、页面

**节点仓库**：
提供可反复拖入画布的节点模板。当前包含提示词生成与优化、剧本创作与优化、剧本转工业级分镜脚本、图片生成、视频生成、视频拼接与合成、网络爆款视频下载，以及爆款视频复刻节点；模板不会因为已经创建过实例而消失或禁用。
_Avoid_: 素材库、节点列表、模型目录

**工作流仓库**：
位于界面最下方、可折叠展开的工作流模板区域。单击模板只会向画布放入一个封装完整业务逻辑的工作流节点；模型选择始终取自项目中已启用的供应商连接与模型绑定。模板本身不保存供应商地址、令牌或外部平台工作流身份。
_Avoid_: 节点仓库、素材库、外部工作流市场

全部七类工作流节点均可添加图片、音频、视频、PDF、TXT、Markdown、JSON 作为多模态参考素材，供文本模型在规划、修订和检查时理解。支持查看文件名、类型、大小及图片缩略图，按路径去重并逐项移除。通用参考素材与商品资料、封面图片共用最多 8 项、单项及合计 14 MiB 的额度；封面专用图片另保留原有 8 MiB 限制。人物图、商品原图与系统质检帧仍保持各自用途。具体可读取的媒体类型取决于所选文本模型及供应商，不支持时明确报错。

参考素材随画布和工作流历史保存本地路径及轻量元数据，正文在实际请求时由后端重新读取和校验，不将内联媒体写入历史。素材内容仅作为参考，不能覆盖用户要求或内置方法。修改或移除参考素材后需重新制作，避免把旧计划和已生成结果当作基于新资料的成果；未添加素材的旧工作流继续兼容断点续跑。

**知识教学视频工作流**：
工作流仓库中的单节点自动化模板。用户只需填写制作要求并在节点内配置项目文本、图片和视频模型；执行后由节点内部完成内容诊断、六段式教学规划、自动审校、封面与分段视频生成、五点抽帧验收以及本地视频合成。普通细节由系统自动推断，只有影响事实、受众或核心表达且无法可靠推断的决策，以及自动质检发现的业务取舍，才暂停并等待一次确认；确认后从已保存检查点继续，不重复提交已完成任务。脚本、分镜、质检记录与最终成片都保留在同一节点内。

规划确认可采用推荐答案或填写自己的决定，已确认答案在规划重试时保留。画幅和逐镜时长按所选模型的能力映射；视觉质检使用独立的严格 JSON 协议，自动返工达到上限后才交给用户确认。应用重启时，未完成的活动工作流恢复为可继续的暂停状态，并保留原任务身份和已有交付物。

**AI影视工作流**：
工作流仓库中的 V1.3 单节点模板，包含概念锚定、角色设定、可选世界观、分场大纲、完整剧本、角色/场景/道具资产、表演设计和视频提示词八阶段。每阶段独立加载内置方法并调用项目文本模型，默认自动推进，只在关键冲突时等待决定。支持按自然语言选择全流程、指定阶段、已有资料接力和范围修订；已有资料可在节点内粘贴，文档模式仅要求文本模型。全流程产出资产图后，将真实本地资产作为视频参考，再复用可恢复的生成、五点视觉质检、返工与本地合成。阶段成果保留版本与依据，修订上游时标记下游过期，旧稿留档；全部文档、资产和成片集中在同一节点。内置方法移除了来源包的作者标记、推广文案、第三方链接和供应商专用设置。
_Avoid_: 第三方算力通道、多节点内部流程、逐步骤人工操作

影视制作文档可从节点一次导出为 Markdown，包含阶段版本、依据、资产和成片路径及逐镜质检记录。暂停后切换到文档交付会停止后续媒体生成；过期资产不参与新镜头生成。续跑时再次检查当前视频模型的时长能力，模型更换造成不兼容时保留进度并提示更新镜头。

**漫剧自动工作流**：
工作流仓库中的单节点模板。用户可在节点内粘贴或导入最多 10 集的 Markdown/文本剧本，设置统一视觉风格、画幅及项目模型。内部逐集执行导演分析、服化道设计、分镜编写；每阶段独立进行业务检查和内容检查，汇总两轮意见自动修订，双通过后才进入下一阶段。自动修订达到上限或出现无法推断的关键冲突时，节点保存进度并等待用户决定，确认后修订并重新检查。人物、场景、道具资产跨集累积复用，同一身份不能被静默改写，场景分别生图以直接用于对应镜头。成片模式按集序生成和合成；文档模式只调用文本模型，按集展示并导出导演讲戏、服化道、分镜和检查记录。重启/暂停后恢复已完成阶段，修改剧本或统一风格后需重新制作。来源包中的品牌、推广链接、外部平台配置及手工指令未进入节点。
_Avoid_: 多节点团队展开、供应商专用分镜节点、每阶段人工确认

**剧情带货工作流**：
工作流仓库中的单节点模板，支持快速 15 秒四镜头与完整五阶段制作。用户添加真实产品图片、商品名称、事实卖点、目标受众及可选来源链接，在节点内选择项目文本、图片和视频模型；完整模式依次进行产品研究、三版创意与自动择优、带货剧本、分镜提示词及一致性资产制作，快速模式一次规划四段剧情并匹配所选视频模型的合法单元时长，总长 15 秒。每阶段独立检查事实、产品推动剧情、原图引用与可执行性，常规问题自动修订，关键事实冲突或修订上限才请求决定，确认后实际修订并重检。

产品来源读取支持最多 4 个公开 HTTP(S) 页面，明确记录读取成功与失败；当前没有通用全网搜索服务，不把未访问链接或包装宣称冒充已核实事实。图片和文档复用项目多模态输入路径，最多 8 项、合计 14 MB。成片模式要求真实商品图；原图不重新生成，直接按固定资产 ID 传入视频参考；角色、场景、虚构道具按一致性四宫格生成。后续复用可恢复的素材生成、视觉质检与本地合成，文档模式只调用文本模型，阶段正文、事实台账、来源、历史版本、资产和成片可在节点内查看及导出。修改产品资料或剧情设置后需重新制作，原稿保留；暂停或重启继续时保留已经完成的步骤。来源包的作者印记、推广链接、固定模型品牌和逐阶段人工闸门不进入工作流。
_Avoid_: 外部供应商配置、虚构产品功效、把网页读取称作全网核验、多节点展开

**动画逻辑图工作流**：
工作流仓库中的单节点模板。用户填写动画描述或 ASCII 草图，只需选择项目内的文本模型。内部先检查本地渲染环境，再匹配十类核心模板或自定义坐标布局，生成声明式动画计划、独立检查并自动修订，最后本地渲染 GIF、MP4 或两者，以及静态预览和可编辑工程。默认 800×600、30fps、8 秒，尾部至少保留 60 帧静止；普通布局自动决定，只有核心事实缺失、关系冲突或自动修订达到上限时才请求确认。确认内容同时用于新一轮规划和检查。

计划、检查意见、历史版本和本地任务身份保存在同一节点。暂停或渲染失败后继续制作会保留已经通过的方案，复用仍在运行的本地任务；已取消、失败或交付文件不完整的任务可重新渲染，不重复调用文本模型。修改描述、模板、尺寸、主题、时长或格式后需重新制作。应用打包包含固定版本的本地渲染引擎、浏览器和编码器，客户无需安装开发环境；可编辑工程单独导出。来源作者标记、推广链接和外部供应商设置不进入内置方法或产品界面，保留底层依赖的许可证。模型输出只作为受校验的数据使用，不执行模型生成的代码。
_Avoid_: AI视频模型生成、远程渲染供应商、多节点展开、要求客户安装开发工具

**小红书封面工作流**：
工作流仓库中的单节点模板。提供同一人物的 1–3 张参考图、选题或文章，可附加最多 5 张产品图、截图等素材（共不超过 8 张、8 MB，为最终图视觉检查预留容量）。仅配置项目内文本模型与支持参考图的图片模型；八种风格可指定或自动匹配，标题可直接填写或自动提炼三个候选后择优。表情、背景、字体等细节可选，默认自动推进，不逐项询问。

内部完成方案、参考图生成、1080×1440 封面处理、标题文字与人物一致性检查，常规问题自动修订；无法确定的事实或修订超限才请求一次决定，确认后实际修订并重检。严格交付 3:4，图片尺寸处理保留完整画面而不裁切标题或人物。封面、方案、提示词与检查历史集中在节点中；也可仅交付完整提示词。暂停与重启保留已有任务身份，修改输入后需重新制作。来源作者标记、推广信息和外部供应商设置不进入产品。
_Avoid_: 默认生成陌生人物、逐项人工选择、展开多个画布节点、近似 3:4、未经检查直接交付

**短视频反推工作流**：
工作流仓库中的单节点模板。用户粘贴一条视频分享链接或选择一个本地视频，只需配置项目内的多模态文本模型。链接下载直接复用项目下载器及其登录凭据；抽帧复用项目的视频联系表：30 秒以内全片每 0.5 秒抽帧，更长视频全片约 12 帧，所有视频最后 5 秒按每秒 6 帧额外加密。联系表带实际时间码并保存到本机，反推与独立复核均读取这些真实图片。

内部产出结构摘要、十维分析、全片固定设定、分场和按动作节拍编写的分镜稿，单独核对结尾动作与定格；同时给出换皮、换视角、换叙事三条完整二创提示词。静态图片不能确认的声音标为待听觉确认，不把模型的推测当成原片事实。普通问题自动修订，必要决定确认后修订与复核。交付原片、Markdown 反推文档、纯文本提示词、代表关键帧及案例文件；案例按本次运行身份幂等入本地库，后续仅以实际已入库案例的统计作为低权重参考，不预装来源包的示例为用户案例。

节点与历史详情保留下载任务、画面证据、分析版本、检查意见及交付路径。下载、分析或入库失败后从相应断点继续，已经完成的步骤和付费模型任务复用。登录凭据由项目现有导入入口处理，来源包的外部解析网站、下载脚本、第三方跳转、作者标记和推广信息不进入工作流。
_Avoid_: 跳转外部下载网站、只按标题编造画面、静态抽帧冒充听过声音、自动修改内置技能文件、多节点展开

**工作流历史记录**：
顶栏历史记录的“工作流”分页统一记录全部七种自动工作流，节点标题栏也可直接打开对应运行。每次重新制作建立独立记录，重试与断点续跑沿用原记录并增加尝试次数。原始输入、项目模型名称、阶段时间线、错误、完整断点与交付物保存在本机 SQLite；内部文本、图片和视频任务通过运行身份精确关联，可继续查看已有任务的请求、响应和报错详情。

保存历史成功后才提交模型调用；断点按顺序写入并使用版本检查避免旧进度覆盖新进度。重启后活动记录恢复为暂停，继续时复用已经完成的成果和已提交任务；未知模型结果不擅自重新计费提交。删除节点或清空画布不会删除运行历史，可从历史恢复节点；若原节点已有新的输入或另一运行，另建节点保留现有内容。失败记录可重试当前步骤，暂停记录可断点续跑，必要决定可在详情内填写并确认；重新制作创建新的运行。历史保存失败会明确显示错误。旧画布首次续跑可从现有断点建立历史，不编造过去未记录的步骤。
_Avoid_: 把失败重新制作称作断点续跑、覆盖旧运行、丢失已生成结果、在历史中存储令牌或内联媒体正文

**生成结果**：
由一次 AI 生成产生的图片或视频，是可以被选择、导出或用于后续创作的视觉内容。
_Avoid_: 文件、节点、作品

**本地生成结果**：
远程图片或视频生成成功后自动保存到当前设备下载目录的生成结果，以本地生成任务、结果索引或远程任务身份关联到生成任务，不属于公司素材库。
_Avoid_: 素材、临时结果、远程素材

**素材库**：
用于浏览和管理可复用图片、视频与音频素材的侧板。可在下拉菜单中选择云端素材或本地素材；两种来源拥有独立的目录与读取链路。
_Avoid_: 文件夹、画布、全部素材

**素材**：
素材库中的一张图片、一段视频或一段音频，可以作为生成请求的参考输入；素材身份始终包含来源类型以及对应的供应商素材 ID 或本地上传任务 ID。
_Avoid_: 文件、附件、生成参数

**本地素材**：
目录索引保存在当前设备 SQLite、媒体正文长期保存在对象存储的素材。上传时不创建云端素材库分组、不调用云端素材导入接口；生成时按本地上传任务 ID 重新签发对象存储读取地址。
_Avoid_: 云端素材、本地生成结果、暂存对象

**暂存对象**：
为了让只接受公网 URL 的远程接口读取本地媒体，或把用户上传文件导入云端素材库，而短期保存的媒体副本。它只承担中转职责，不是素材或本地生成结果，也不出现在画布中。
_Avoid_: 素材、生成结果、缓存文件

**素材节点**：
素材在画布中的可连接实例，用于把图片、视频或音频传递给生成节点。同一素材可以在画布中创建任意多个彼此独立的素材节点实例。
_Avoid_: 上传节点、文件节点

**本地结果节点**：
本地生成结果在画布中的可预览引用，以对应生成任务、结果索引和本地文件为身份，不要求存在公司素材记录。
_Avoid_: 素材节点、下载节点、远程结果节点

**媒体引用**：
生成节点提示词中通过 `@` 插入的结构化引用，唯一指向已经连入该生成节点的某个素材节点实例；即使多个实例引用同一来源素材也不会混淆。显示名称、预览地址或文件路径的变化不改变其身份。
_Avoid_: @文字、素材名称、文件链接

提示内容的统一规则见 [提示词引用规则](docs/prompt-references.md)：默认只识别带 `@` 的明确引用；裸名称由用户点击“识别素材名”批量处理。同名必须确认当前项，不推断之后输入。

**媒体位置**：
一次生成请求中某个媒体输入在同类型媒体序列内从 `1` 开始的位置。全部已连线素材实例按当前连线顺序占据位置，每个实例只占一个位置；`@` 的出现先后不改变媒体编号，图片、视频和音频分别拥有独立顺序。
_Avoid_: 全局素材位置、文件顺序、完成顺序

**提示内容**：
保存在图片或视频生成节点提示词输入框内，由有序文字片段和媒体引用组成、可以由该生成节点按自身协议解释的创作输入；提示词节点的源输入与生成输出是纯文本。
_Avoid_: 提示词字符串、富文本、请求体

**生成节点**：
从节点仓库拖入画布、可重复创建且不限制实例数量的提示词、图片或视频生成操作。提示词节点调用文本模型生成/优化提示内容，支持多轮对话（每轮生成或优化都会携带该节点此前的全部对话），可接收图片素材以及已保存的图片/视频产物连线做多模态参考理解，并可通过提示词连线下发到图片或视频生成节点；媒体生成节点接收兼容素材连线并产生生成结果。
_Avoid_: 模型节点、请求节点、工具节点

**MiniMax H3 专用提示词技能**：
提示词节点的 `minimax_h3` 模式内置 `h3-ref2va-optimizer` V1.6（2026-09-05），用于 Ref2VA 全参考提示词生成与优化。完整技能、八份参考文档、版本说明与校验器源码随应用编译，每轮请求重新注入，并沿用节点的完整对话和实际参考素材。六段式字段名、引用标签及保留标记使用英文，叙述默认使用中文；输出保留读图校准表、图片上传顺序、无图确认与补图说明、多段整体设定及尾帧衔接。

该模式已替换旧 `minimax-h3-prompt`，不再读取桌面技能目录，也不包含旧版全模式回退。原包的 Python 校验器与样例保留在项目内；当前文本模型调用根据规则自检，不执行 Python，也不调用包中提及但未内置的官方技能。
_Avoid_: 旧版三字段英文提示词、全 H3 模式自动路由、声称已运行 Python 校验器

**FPV 路径提示词技能**：
提示词生成与优化节点的 `fpv_path` 模式。连接带红线或箭头的场景图后可直接生成，也可用文字描述起点、途经点和终点；读图使用项目中支持图片理解的文本模型。每轮加载随应用编译的路径方法，并携带完整对话、实际参考图和当前可编辑输出，支持继续优化。交付路径调整说明（如有）、时长建议、参考图版与纯文字版提示词、运镜时间线和速度节奏，完整结果保留在节点中并沿用现有输出连线。

默认按 15 秒规划，可按用户要求调整；复杂路线优先分段并说明衔接，不擅自删除关键途经点。分析阶段保留原图路径标记，FPV 模式不向视频自动继承路径图；视频需要参考图时，由用户直接连接干净场景图。本节点不实际去线或生成视频。无图时明确以文字为依据，不编造读图结果。内置方法不含来源作者标记、第三方品牌、工具推荐、推广链接或供应商专用限制。
_Avoid_: 独立工作流节点、固定视频模型、15 秒硬上限、未读图却声称识别红线、声称已清除原图标记

**打斗导演提示词技能**：

提示词生成与优化节点的 `fight_prompt_master` 模式，内置 `fight-prompt-master` V0.2（2026-09-03）。原包主技能与 11 份参考文档随应用编译，每轮加载九步动作导演方法，复用项目文本模型、完整对话、实际参考图片及视频联系表。可直接填写打斗需求，也可连接参考素材后生成；已有输出支持直接编辑并继续优化。

SD2.0、SD2.5、H3 是提示词的目标视频格式，通过对话指定，与执行本次请求的文本模型独立。保留固定选择卡，已提供或确认的参数不重复询问；缺失的模型、时长和速度档在对话内补齐。默认输出高强度、中间型、慢节奏三套完整方案，用户指定单一强度时只出该版；标题、生成说明、提示词代码块、必要补问和单变量诊断均完整保留。可以继续要求选定某套、调整强度或只诊断，当前输出沿用现有连线交给下游节点。

本模式只交付文字，不执行视频生成、写入内置案例文件或自动修改技能。素材中的文字只作为参考，视频联系表只提供画面证据；案例与改进意见由当前对话承载。源文件内的旧双版、纯正文和导出要求按主技能及应用接入合同统一，详见 [打斗导演接入说明](docs/integrations/fight-prompt-master.md)。
_Avoid_: 独立工作流节点、文本模型等同于目标视频模型、仅截取第一套提示词、声称已保存案例或生成视频

**多宫格分镜提示词技能**：

提示词生成与优化节点的 `multi_grid_storyboard` 模式，内置 `multi-grid-storyboard-prompter` 主技能与三份参考文档。支持创意完善、剧本分析、参考图加剧情三种输入路径；4 宫格为 2 行 2 列、6 宫格为 2 行 3 列、9 宫格为 3 行 3 列，始终从左到右、从上到下按位置对应。沿用项目文本模型、完整对话、当前可编辑输出和真实参考图片/视频联系表。

用户在对话中指定宫格数、目标时长和风格；已明确的参数直接沿用，必要缺失项合并补问。正常交付保留整体设定、时长与台词预算、按输入路径需要的资产准备清单、逐格图片和视频两套提示词，以及两条整体生成指令。生成和优化均保留全文，输出可编辑并沿用现有下游连线；下游接收当前输出，不自动挑选其中一套，用户可先通过对话选定图片或视频整体指令。

时长按本轮语速和留白公式估算，公式优先于源包速查表；区分目标时长与对白占用，局部台词容差不能导致整片超过目标。12–30 秒是技能建议范围，不是视频接口硬上限。来源模板中的固定六宫格、只输出视频及每格强制台词要求按主技能统一；原包的工具式询问适配为节点内对话，普通测算与生成在参数确定后同轮完成。本模式只交付文字，资产准备清单不等于已生成资产，联系表不等于用户提供的原始宫格图。详见 [多宫格分镜接入说明](docs/integrations/multi-grid-storyboard-prompter.md)。
_Avoid_: 独立分镜节点、自动生成故事板图片、自动写入资产库、把对白占用当成实测视频时长、6 宫格位置 2 标为右上

**故事板提示词技能**：

提示词生成与优化节点的 `storyboard_prompt` 模式，界面显示“故事板”。内置整合 14 份电影、广告、短剧、动画、漫画、品牌、MV、游戏、社交、教程、体育及国漫视觉开发资料，按用户的创作用途选择相应方法，交付可直接交给图片节点的完整整张故事板提示词，包含版式、逐格画面和跨格一致性。复用项目文本模型、完整对话、真实参考图片与已保存视频，当前输出可直接编辑并继续优化，随画布恢复并沿用现有下游连线。

只提供文字需求也可生成；仅连接参考素材时，以实际可见内容为依据并区分已有剧情与创作建议。故事板图片由用户连接图片节点后执行生成；本模式与剧本转工业级分镜脚本节点保持独立。详见 [故事板提示词接入说明](docs/integrations/storyboard-prompt.md)。
_Avoid_: 自动出图、将视频联系表称作原始故事板、将未提供的剧情或参考图说成已确认事实

**供应商连接**：
一条可被生成节点选择的远程生成能力访问路径，具有独立的显示身份和凭据作用域；多个供应商连接可以提供同一个模型。
_Avoid_: 模型、API Key、供应商账号

**模型定义**：
一个不依赖具体供应商连接的生成模型能力描述，规定可用输入、参数和生成结果类型。
_Avoid_: 供应商、节点模板、API 配置

**供应商模型绑定**：
一个供应商连接提供某个模型定义的可用关系；同一模型定义可以同时绑定到多个供应商连接。
_Avoid_: 模型定义、供应商连接、模型副本

**图片生成节点**：
节点仓库中的图片生成模板。节点内自带提示词输入框，可以执行纯文字图片生成或使用有序图片素材执行图片参考生成；MVP 使用完整图片作为参考输入，不提供局部遮罩编辑。
_Avoid_: 文生图节点、图生图节点、模型节点、把图片生成节点称作提示词节点

**视频生成节点**：
节点仓库中的视频生成模板。节点内自带提示词输入框，头部的向右播放图标是创建视频生成任务的执行入口。
_Avoid_: 播放器节点、模型节点、把视频生成节点称作提示词节点或任务节点

**视频拼接与合成节点**：
节点仓库中的本地视频工具节点。它接收两段或更多视频素材节点或视频产物卡片的连线，按节点内明确展示且可调整的顺序直切合成为完整视频；输出自动保存到本机并作为新的视频产物卡片落在右侧。它不选择供应商或模型，也不创建远程生成任务。
_Avoid_: 视频生成节点、剪辑时间线、远程合成任务

**网络爆款视频下载节点**：
节点仓库中的本地下载工具节点。它把用户粘贴的网络视频链接（支持抖音分享口令整段粘贴）交给应用内置的 yt-dlp 引擎下载，产物自动保存到本机下载目录并作为新的视频产物卡片落在右侧。引擎二进制由应用自动获取、校验官方 SHA-256 并可随最新稳定版更新；抖音下载默认携带用户导入的浏览器 Cookies。它不接收媒体连线，不选择供应商或模型，也不创建远程生成任务。
_Avoid_: yt-dlp 节点、爬虫节点、远程下载服务、视频生成节点

**爆款视频复刻节点**：
节点仓库中的多模态视频分析节点。它内置 `douyin-reverse-prompt` V1.1 的纯复刻适配版，只保留密集抽帧、逐帧视觉反推、逐秒生成提示词、爆点诊断与三条二创路线；不包含下载、链接解析、案例写盘或自动学习执行。它可直接连接网络爆款视频下载节点，并在该节点下载完成后自动使用最新本地视频；也可连接视频素材或已有视频产物。运行时在本地 WebView 中按短视频每 0.5 秒一帧、结尾 5 秒每秒 6 帧的规则生成带时间码联系表，再交给用户选择的多模态文本模型，输出可编辑、可导出的 Markdown 复刻方案。
_Avoid_: 视频下载节点、视频生成节点、搬运节点、把联系表时间码当成原视频字幕

**剧本创作与优化节点**：
节点仓库中的独立文本创作节点。它内置中文短剧与通用编剧双技能，每轮请求都会重新注入技能全文以及该节点此前的全部对话；当前 Markdown 剧本可直接编辑并一键导出。它不接收媒体连线，也不向图片或视频生成节点下发提示内容。
_Avoid_: 提示词节点、剧本生成任务、媒体生成节点

**剧本转工业级分镜脚本节点**：
节点仓库中的独立分镜文本节点。它内置 `viral-video-prompt-engine` V4.6 完整技能（SKILL.md 与 16 份 reference 文档），把已有剧本逐轮转化或修订为可执行的 Markdown 工业级分镜脚本；每轮请求都会重新注入技能全文、该节点此前的全部对话以及当前分镜稿。当前分镜稿可直接编辑并一键导出。它不接收媒体连线，也不向图片或视频生成节点下发提示内容。
_Avoid_: 剧本创作与优化节点、视频生成节点、故事板图片节点、提示词节点

**生成任务**：
用户每次执行生成节点后立即形成的独立、持久异步记录，保存冻结请求、全部执行尝试、返回信息以及结果保存状态；成功或失败等终态不改变其保存要求。
_Avoid_: 临时任务、节点、远程生成任务、生成结果

**执行尝试**：
生成任务使用同一份冻结快照向供应商执行的一次实际调用；初次调用和自动重试是同一生成任务下不同的执行尝试。
_Avoid_: 生成任务、用户重试、远程生成任务

**远程生成任务**：
供应商接受异步生成后返回的可查询运行记录，具有供应商范围内的远程身份；并非每笔本地生成任务都有远程生成任务。
_Avoid_: 生成任务、本地任务、节点

**令牌**：
决定远程素材和生成任务访问范围的凭据作用域；同一用户的不同令牌彼此隔离。
_Avoid_: 用户、账户、素材库
