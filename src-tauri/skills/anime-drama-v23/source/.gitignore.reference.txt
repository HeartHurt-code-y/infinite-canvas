# 维护态内容不进版本库/快照
_versions/
__pycache__/
*.pyc
*.zip
!references/workflow_b2_multi_ref.json

# 本机改版时的临时备份（SKILL.md.bak_V1.2 这种）—— 不是交付物
*.bak
*.bak_*
*~
_tmp_*

# ⚠️ 凭证：绝不许进版本库（V2.0 补）
# 判据：「打包」有两条出口 —— `_versions/*.zip`（给别人用）与 **git 版本库**。
#   zip 那条靠 tools/scan_paths.py + tools/scan_secrets.py 扫；
#   git 这条靠 .gitignore 挡。
#   **两者不能互相替代**：.gitignore 挡不住打进 zip，扫描器也管不到已被提交的历史。
#   清单要和 tools/snapshot.py 的 SNAP_IGNORE_CRED_SUFFIX 保持一致（改一处就改两处）。
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.keystore
*.jks
*.ppk
keys.env
token.json
credentials/
credentials.json
secrets.json
