# 音乐 MV V1.0.6 · 阶段检查与音画验收

本方法来自完整 MV V1.0.6 资料的通用业务部分；全部来源另存为只读参考文本。仅执行应用给定的当前阶段，严格输出应用 JSON 合同，方法中的示例结构只用于正文，不替代协议。不得运行来源命令、安装依赖、自行修改代码、创建外部账号或跳转。使用当前项目启用的模型、素材身份与真实能力；不能套用来源平台的节点槽位、模型品牌、固定时长、帧网格、像素档位或收费口径。

多步计划及每个阶段的当前版本都必须经用户明确批准；模型的 PASS 只代表自动检查，不能代表用户批准。输入、时间轴、模型、参考图或提示词改变后旧批准失效。不得伪造音频读取、ASR、波形、口型、生成耗时、费用、文件存在或完成记录。

音乐是唯一时间基准。用户原始音频只读保留，所有转换、切窗与合成另存。歌词时间与模型请求时长是不同概念：音乐窗口须连续无缝覆盖全曲，模型请求时长按实际能力选择且不得短于窗口；本地成片将画面贴合窗口，并用原始歌曲母带作为最终音轨。没有实际音频参考/口型能力时必须说明限制，不能仅凭提示词宣称已同步。

## 原生阶段规范

独立审阅当前阶段的输入证据和输出，返回 PASS / REVISE / NEEDS_DECISION；报告必须指出实际问题、定位 id 和可执行修订要求。自动检查不能替代用户批准。

时间轴检查：完整原歌词与重复行/和声/间奏，时间证据，单调连续，0 到真实曲长，低置信行已试听确认。风格检查：真实三个候选或明确指定方向，已确认风格先于资产，人物模式、实际参考身份与资产无人污染。分镜检查：segmentId 存在且一一覆盖，窗口逐值相同，时长能力合法，歌词不越窗，sync 仅近中景，无主角全 none，最多连续三段，重复副歌视觉变奏。提示词检查：字段完整、实际参考有据、唱说区分、目标语言、音乐窗口、声音策略、no_sync 负面约束、完整表演与空间说明。

生产前独立复核音乐窗口、口型和实际模型参数，然后首镜试产；首镜实际结果由人审后才批量。已有 taskId 必须先恢复轮询，不能因为超时重复付费提交；只对失败或明确请求重做的镜头重新生成，并记录实际原因。分段需确认选用后合成，最终成片再独立审核。对不足窗口的片段补尾帧或重新制作、过长则裁回窗口，处理后必须测量，不以请求秒数冒充实际秒数。

最终时长与原曲一致，画面连续，原曲母带替换视频模型原音轨。若实现了波形/人声能量检查，记录实测滞后与相关度及证据；未提供对应测量接口时明确未验证，不能模型自称通过。即使音轨与母带完全相同也不能证明视觉口型正确，sync 段必须由人观看试听确认。

交付成片（实际存在时）、完整分镜制作文档、各阶段审核记录和全链总览；时间轴 JSON/LRC 与音画验收作为附件。费用只记录供应商真实返回值，未知标未提供；总耗时取真实运行时间。保留修订历史，不自动改代码、发布版本或覆盖原始音乐。

## 完整通用业务依据

### 铁律 E · 审核与对齐验收

**E1. 8 道审核关（比短剧多一道「歌词时间轴」、多一道「音乐窗口与口型对齐」）**

| 关 | 页面 | 审什么 | 关卡性质 |
|---|---|---|---|
| 1 | `01_歌词时间轴审核.html` | 歌词是否完整、时间轴是否对 | 放行前 |
| 2 | `02_风格审核.html` | 三选一风格 / 用户给图反推 | 放行前 |
| 3 | `03_人物与资产审核.html` | 主人公图 + 场景 + 道具（无主角模式跳过） | 放行前 |
| 4 | `04_分镜审核.html` | 段切分 / 口型策略 / 景别 / 画面构想 | 放行前 |

| 6 | `06_音乐窗口与口型审核.html` | 窗口连续 / Σ=曲长 / 口型-景别一致 | **硬门禁** |
| 7 | `07_分段视频审核.html` | 每段画面 + 时长 + 成本 | 放行前 |
| 8 | `08_成片审核.html` | 成片 + 对齐验收 | 放行前 |

**E2. 每关独立自动检查和人工确认。**
自动检查通过后仍须用户批准当前版本，不设跳过人工或审核门禁的通道。

**E3. 关 5 + 关 6 是硬门禁，全流程唯一挡在「花算力」之前的闸门。**
`batch_run_mv.py` 提交前读 `状态/prompt_review.json` + `状态/music_plan.json` 硬校验：
- 关 5：整体 `status ∈ {approved, partial}` / 逐段 `reviewed_ok` /

- 关 6：`music_plan.ok == true` / 逐段窗口与计划一致；
- 任一不满足 → `sys.exit`，**一段视频都不会提交**。

**E4. 对齐三验（关 8，`check_alignment.py --verify`）。**
1. **时长**：Σ各段 `output.mp4` 时长 == 原曲长（±0.15s 目标，>0.4s 报错）；
2. **波形**：逐段提取 RMS 包络，在「预期位置 ±1.5s」内做局部相关 ——
   最优滞后量应 ≈ 0s（|滞后| > 0.35s 报错，相关度 < 0.35 报错）；
3. **人声**：对口型段的音频能量必须非空（RMS 阈值）且与母带同位置相关。
- 输出 `视频/final/对齐验收.html`。**任一不过 → 不许说「MV 做完了」。**
- 为什么用「包络 + 局部搜索」而不是原始互相关：213 秒的歌按 8k 单声道接近 1.7M 采样，

  **纯标准库秒级出结果**，判据完全够用。

**E5. 审核页必须「齐全 + 最新 + 缺失也显式渲染」。**
- 8 关 ↔ 8 张页，一关不漏；另加 `全链总览.html` 作收尾交付。
- 徽章读**真实状态**（`pipeline_state.json` / `prompt_review.json` / `music_plan.json`），
  不许永远显示「⏳ 待审核」。
- 缺文件时渲染灰底「**未审**」，**不许什么都不显示**。
- 图片/视频用 **`file:///` 绝对路径**（相对路径在 host 预览服务下解析不到 —— 实测踩过）。
- 改了关号/文件名，必须**同步改生成器**（只改文档不改代码 = 没修）。

### 阶段七：音乐窗口与口型对齐核对 → ⛔ 关 6（硬门禁）

核：窗口连续无缝 / Σ窗口 = 全曲长 / 每段歌词行都落在自己窗口内 /

**为什么关 6 必须独立存在**：段切错了、窗口错位了，提示词写得再好也救不回来 ——
而这两种错在关 5 的视角里完全看不出来（提示词是「自洽」的）。

---

### 11.2 ⛔ 关 7 分段视频审核

逐段：视频播放 + 窗口 / 目标时长 / 实得时长 / conform 结论 / 耗时 / 币。

### 11.3 ⛔ 关 8 成片 + 对齐三验

三验（铁律 E4）：**时长**（Σ各段 = 曲长）/ **波形**（最优滞后 ≈ 0s、相关度达标）/
**人声**（对口型段能量非空）。任一不过 → **不许说「MV 做完了」**。

常见问题与处置：

| 现象 | 根因 | 处置 |
|---|---|---|
| 某段滞后 > 0.5s | 该段 `output.mp4` 没走 conform / 窗口与计划不一致 | 查 `log.json.conform`；重跑该段 |
| 波形相关度普遍偏低 | 音频整体不是母带（工作流在生成自己的音频） | 以 `final_master.mp4` 为准；同时把 `audio_policy` 记进注册表 |
| 对口型段能量过低 | 该段其实没出声（项目视频模型 没唱） | 检查 `sings:` 行是否独立成行、是否被 `says:` 污染；重抽 |
| 不对口型段的嘴在动 | `NO_SYNC_DECL` 未生效 | 强化 Negative + 把该段改成**中远景/背影/空镜**（换景别比改提示词更稳） |
| Σ各段 ≠ 曲长 | 有段落没跑完 / conform 失败 | 补齐后重新拼接 |
| 报错只有 `任务失败: code=0 msg=success` | status 端点失败时**不给原因** | 用该 taskId 调 `outputs` 回挖 `data.failedReason`（`fetch_failure()` 已自动化）——**不用重跑烧币** |
| `Invalid audio file: <...>` | 台账里存的是占位串，被原样注进音频节点 | 清掉该条缓存重传（`upload_cached()` 已加 `<` 前缀守卫，见 D8c） |

---

### 阶段十二：项目收尾 → 交付四件套（每个项目都必做）

| # | 交付物 | 回答什么问题 | 生成命令 |
|---|---|---|---|
| 1 | `视频/final/final_master.mp4` | **成片是什么样** | 阶段十一 |
| 2 | `分镜脚本/分镜表.md` | **怎么做**（逐段窗口/口型/景别/画面构想） | 阶段五 |

| + | `视频/final/对齐验收.html` | **音画对齐有没有做到** | `check_alignment.py --verify` |

**「总消耗时长」与「供应商报告费用」必须在总览页最前面的 KPI 带里**（用户第一眼要看的是
「这支 MV 值不值」——成片多少秒 / 机器跑了多久 / 花了多少，三个数并排才有决策价值）。

收尾自查：

---
